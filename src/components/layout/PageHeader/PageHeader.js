import React from "react";
import { StyledCardHeader } from "./PageHeaderStyled";

const PageHeader = ({ title }) => {
  return <StyledCardHeader title={title}></StyledCardHeader>;
};

export default PageHeader;